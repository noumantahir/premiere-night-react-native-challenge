import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 16,
    marginTop: 16,
  },
  backdrop: {
    height: 150,
    width: '100%',
    borderRadius: 12,
    resizeMode: 'cover',
  },
});


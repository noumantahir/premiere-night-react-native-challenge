import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 16,
    marginTop: 16,
    borderRadius: 12,
    backgroundColor: '#f2f2f2',
    overflow: 'hidden',
  },
  backdrop: {
    height: 150,
    width: '100%',

    resizeMode: 'cover',
  },
});

